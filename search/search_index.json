{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"AAAX","text":"<p>aaax.one</p> <p>AAAX is the PSI shell for package-shaped agent systems. It gives packages a place to become useful: inspect a manifest, mount its tactics and channels, bind local handlers, open a FastAPI surface, and hand the resulting environment to a human, script, or coding agent.</p> $ aaax inspect packages/analyst-pack mounted tactic echo -&gt; psi://demo/analyst-pack/tactics/echo mounted channel events -&gt; psi://demo/analyst-pack/channels/events $ aaax serve packages/analyst-pack --port 8400 listening http://127.0.0.1:8400 <p>AAAX stays thin on purpose. LLLM owns tactic execution, SSSN owns semantic channels, PsiHub owns package protocol and storage, and agent tools own their reasoning loops. AAAX is the shell where those resources get named, mounted, served, and passed along.</p> Inspect     Read a package or strategy and see the local command surface before serving it.    Mount     Mount `psi.toml` resources into one shell without erasing their `psi://` refs.    Pipe     Call tactics, append/query channels, and invoke resources through stable endpoints.    Handoff     Preserve cards, docs, examples, assets, and service URLs for people and agents."},{"location":"#fast-path","title":"Fast Path","text":"<p>Serve a package:</p> <pre><code>aaax serve packages/analyst-pack --port 8400\n</code></pre> <p>Call a tactic:</p> <pre><code>curl -X POST http://127.0.0.1:8400/tactics/echo/run \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"text\": \"hello\"}, \"context\": {\"request\": \"demo\"}}'\n</code></pre> <p>Write and read a channel:</p> <pre><code>curl -X POST http://127.0.0.1:8400/channels/events/events \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"kind\": \"record\", \"payload\": {\"text\": \"hello\"}}}'\n\ncurl 'http://127.0.0.1:8400/channels/events/events?limit=10'\n</code></pre>"},{"location":"#shape","title":"Shape","text":"flowchart LR   A[\"PsiHub package\"] --&gt; B[\"AAAX shell\"]   C[\"strategy.py\"] --&gt; B   B --&gt; D[\"resource table\"]   B --&gt; E[\"FastAPI surface\"]   E --&gt; F[\"LLLM tactics\"]   E --&gt; G[\"SSSN channels\"]   D --&gt; H[\"human or agent handoff\"] <p>AAAX treats a package like something you mount into a shell session. The local name can change, but the package ref remains stable. A caller can list what is mounted, run one tactic, append an event, invoke a service, or call the shell's top-level <code>/run</code> command.</p>"},{"location":"#what-aaax-owns","title":"What AAAX Owns","text":"<ul> <li><code>Strategy</code>, <code>StrategyResource</code>, and the shell's mounted resource table.</li> <li>Loading <code>strategy.py</code>, <code>aaax.py</code>, <code>module:factory</code>, package folders, and   direct <code>psi.toml</code> files.</li> <li>Mounting PsiHub package resources into one local namespace.</li> <li>Binding Python tactic entrypoints through LLLM when available.</li> <li>Opening SSSN-backed local channel endpoints.</li> <li>Serving the shell through FastAPI and the <code>aaax</code> CLI.</li> </ul>"},{"location":"#what-stays-outside","title":"What Stays Outside","text":"<ul> <li>LLM provider execution, tool policy, traces, evals, and runtime internals.</li> <li>Package indexing, package upload/download, validation reports, and hub storage.</li> <li>Durable production storage, deployment, queueing, and service orchestration.</li> <li>Coding-agent behavior itself. AAAX gives agents a shell surface; it does not   decide how they reason.</li> </ul>"},{"location":"#next","title":"Next","text":"<ul> <li>Start with Getting Started.</li> <li>Learn the model in Strategies.</li> <li>Mount packages in PsiHub Packages.</li> <li>Serve your first package in Serve A Package.</li> </ul>"},{"location":"getting-started/","title":"Getting Started","text":"<p>AAAX starts like a shell: point it at something package-shaped, inspect what it can mount, then serve the command surface. Use a package folder when one package already describes the system. Use a strategy file when you want a shell script that mounts several packages or adds a top-level <code>/run</code> command.</p>"},{"location":"getting-started/#install","title":"Install","text":"<pre><code>python -m pip install aaax\n</code></pre> <p>For package and channel binding, install the PSI stack integration packages:</p> <pre><code>python -m pip install \"aaax[integrations]\" lllm-core sssn psihub\n</code></pre>"},{"location":"getting-started/#mount-and-serve-a-package","title":"Mount And Serve A Package","text":"<p>Create a minimal package:</p> <pre><code>analyst-pack/\n  psi.toml\n  demo/\n    __init__.py\n    tactics.py\n</code></pre> analyst-pack/psi.toml<pre><code>[package]\npsi_version = \"0.1\"\norg = \"demo\"\nname = \"analyst-pack\"\nversion = \"0.1.0\"\nkind = \"app\"\nprimary = \"runs.local\"\ndescription = \"Demo analyst package.\"\n\n[tactics.echo]\nentry = \"demo.tactics:echo\"\nruntime = \"python\"\ndescription = \"Echo one payload.\"\n\n[channels.events]\nform = \"log\"\ndescription = \"Application event stream.\"\n\n[services.api]\ntactic = \"echo\"\ntransport = \"fastapi\"\ndescription = \"HTTP-facing echo service.\"\n\n[runs.local]\ntactics = [\"echo\"]\nchannels = [\"events\"]\nservices = [\"api\"]\n</code></pre> analyst-pack/demo/tactics.py<pre><code>def echo(input_value, *, context=None):\n    metadata = getattr(context, \"metadata\", {})\n    return {\"echo\": input_value, \"metadata\": metadata}\n</code></pre> <p>Enter the shell:</p> <pre><code>aaax inspect analyst-pack\naaax serve analyst-pack --port 8400\n</code></pre> <p>Call it:</p> <pre><code>curl -X POST http://127.0.0.1:8400/tactics/echo/run \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"text\": \"hello\"}, \"context\": {\"request\": \"quickstart\"}}'\n</code></pre>"},{"location":"getting-started/#write-a-shell-script","title":"Write A Shell Script","text":"<p>When one package is not enough, use a strategy file:</p> strategy.py<pre><code>from aaax import Strategy\n\n\ndef build_strategy() -&gt; Strategy:\n    shell = Strategy(\n        \"analysis-shell\",\n        description=\"Mount source channels and analyst tactics.\",\n    )\n    shell.use_package(\"packages/source-channels\", prefix=\"sources\")\n    shell.use_package(\"packages/analyst-tactics\", prefix=\"analysts\")\n\n    @shell.runner\n    def run(input_value, *, context=None):\n        return {\n            \"input\": input_value,\n            \"resources\": [resource.ref for resource in shell.resources],\n            \"context\": context or {},\n        }\n\n    return shell\n</code></pre> <pre><code>aaax serve strategy.py --port 8400\n</code></pre> <p>Prefixes keep mounted names distinct while preserving original <code>psi://</code> refs in metadata.</p>"},{"location":"getting-started/#inspect-before-serving","title":"Inspect Before Serving","text":"<pre><code>aaax inspect analyst-pack\naaax inspect strategy.py --json\n</code></pre> <p>Inspection is the shell habit: check the local names before you run anything. Those names become <code>/resources/{name}/invoke</code>, <code>/tactics/{name}/run</code>, and <code>/channels/{name}/events</code>.</p>"},{"location":"composition/","title":"Strategy","text":"<p>Strategy is the main reason AAAX exists. LLLM owns tactic boundaries, SSSN owns channels and stores, and PsiHub owns package manifests. AAAX gathers those resources into one shell and exposes them as one command surface.</p> DescribeWrite `psi.toml` resources. MountLoad the package into a shell. BindAttach local tactic and channel handlers. ServeExpose the shell as FastAPI."},{"location":"composition/#resource-kinds","title":"Resource Kinds","text":"<p>AAAX understands the package-facing resource kinds used by the PSI stack:</p> <ul> <li>package</li> <li>schema</li> <li>tactic</li> <li>channel</li> <li>snapshot</li> <li>service</li> <li>run</li> <li>config</li> <li>doc</li> <li>example</li> <li>asset</li> <li>custom</li> </ul> <p>The built-in package bridge imports all of the PsiHub manifest sections that matter for launch context, not only executable tactics.</p>"},{"location":"composition/#strategy-rules","title":"Strategy Rules","text":"<ul> <li>Local names must be path-segment friendly.</li> <li>Prefix imported packages when names might collide.</li> <li>Keep original <code>psi://</code> refs in resource records.</li> <li>Treat channels as resources with shell-facing handlers, not hidden runtime state.</li> <li>Treat docs, examples, assets, package cards, and config as first-class context.</li> </ul>"},{"location":"composition/#next","title":"Next","text":"<ul> <li>See the exact resource record in Strategy Resources.</li> <li>Learn manifest import behavior in PsiHub Packages.</li> <li>Learn channel and service binding in Channels And Services.</li> </ul>"},{"location":"composition/channels-services/","title":"Channels And Services","text":"<p>AAAX makes channels and services available through the same shell boundary as tactics. This lets a mounted package expose both \"do work\" and \"share state\" operations without inventing a separate API surface.</p>"},{"location":"composition/channels-services/#channels","title":"Channels","text":"<p>A package channel:</p> <pre><code>[channels.events]\nform = \"log\"\ndescription = \"Application event stream.\"\n</code></pre> <p>becomes:</p> <ul> <li>a <code>channel</code> resource in <code>/resources</code>;</li> <li>an item in <code>/channels</code>;</li> <li>a local SSSN channel in the bound store;</li> <li>an append/query HTTP endpoint.</li> </ul> <p>Append:</p> <pre><code>curl -X POST http://127.0.0.1:8400/channels/events/events \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"kind\": \"record\", \"payload\": {\"id\": \"a1\"}}}'\n</code></pre> <p>Query:</p> <pre><code>curl 'http://127.0.0.1:8400/channels/events/events?after_cursor=0&amp;limit=100'\n</code></pre>"},{"location":"composition/channels-services/#channel-operations","title":"Channel Operations","text":"<p>The generic resource invocation path also supports channel operations:</p> <pre><code>curl -X POST http://127.0.0.1:8400/resources/events/invoke \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"op\": \"info\"}}'\n</code></pre> <p>Supported operations are:</p> <ul> <li><code>info</code></li> <li><code>append</code></li> <li><code>query</code></li> </ul> <p>The HTTP convenience endpoints set <code>op</code> for you.</p>"},{"location":"composition/channels-services/#services","title":"Services","text":"<p>A service can point at a single tactic:</p> <pre><code>[services.api]\ntactic = \"echo\"\ntransport = \"fastapi\"\ndescription = \"HTTP-facing echo service.\"\n</code></pre> <p>AAAX imports it as a <code>service</code> resource. If the tactic is locally bound, the service resource uses the same handler:</p> <pre><code>curl -X POST http://127.0.0.1:8400/resources/api/invoke \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"text\": \"hello\"}}'\n</code></pre> <p>This is intentionally conservative. AAAX does not launch arbitrary service processes from a manifest. It exposes the shell-facing resource boundary and lets deployment or agent tooling decide how far to go.</p>"},{"location":"composition/psihub-packages/","title":"PsiHub Packages","text":"<p>AAAX can mount a package folder or a direct <code>psi.toml</code> file:</p> <pre><code>from aaax import load_strategy\n\n\nshell = load_strategy(\"packages/analyst-pack\")\nshell = load_strategy(\"packages/analyst-pack/psi.toml\")\n</code></pre> <p>The loader recognizes a package after it checks for <code>strategy.py</code> and <code>aaax.py</code> in a directory. That keeps explicit strategy files in control when both exist.</p>"},{"location":"composition/psihub-packages/#imported-manifest-sections","title":"Imported Manifest Sections","text":"<p>AAAX imports:</p> <ul> <li><code>[package]</code> as a package resource and shell metadata.</li> <li><code>[card]</code> into package and strategy metadata.</li> <li><code>[config]</code> as <code>config.default</code>.</li> <li><code>[schemas.*]</code> as schema resources.</li> <li><code>[tactics.*]</code> as tactic resources and local handlers when bound.</li> <li><code>[channels.*]</code> as channel resources and local SSSN handlers when bound.</li> <li><code>[snapshots.*]</code> as snapshot resources.</li> <li><code>[services.*]</code> as service resources.</li> <li><code>[runs.*]</code> as run resources.</li> <li><code>[docs.*]</code>, <code>[examples.*]</code>, and <code>[assets.*]</code> as context resources.</li> </ul>"},{"location":"composition/psihub-packages/#tactic-entrypoints","title":"Tactic Entrypoints","text":"<p>AAAX imports the Python value named by <code>entry</code>:</p> <pre><code>[tactics.echo]\nentry = \"demo.tactics:echo\"\nruntime = \"python\"\n</code></pre> <p>It supports:</p> <ul> <li>LLLM <code>Tactic</code> subclasses;</li> <li>LLLM <code>Tactic</code> instances;</li> <li>normal Python callables;</li> <li>zero-argument factories that return one of the above;</li> <li>objects with a <code>run</code> method.</li> </ul> <p>When LLLM is installed, normal callables and <code>run</code> methods are wrapped through <code>as_tactic</code>, so callers meet the same <code>CallContext</code> and validation boundary.</p>"},{"location":"composition/psihub-packages/#channel-stores","title":"Channel Stores","text":"<p>When a package has channels and <code>bind=True</code>, AAAX creates a local SSSN store:</p> <pre><code>shell.use_package(\"packages/source\", store=\".aaax/source-store\")\n</code></pre> <p>If no store path is provided, the package folder gets a <code>.sssn</code> store. Existing channels are reused.</p>"},{"location":"composition/psihub-packages/#package-cards","title":"Package Cards","text":"<p>Cards are not execution code, but they matter. AAAX preserves card summaries, tags, safety notes, latency notes, suggested commands, and metadata so future agent handoff surfaces can inject useful package context without re-parsing the manifest.</p>"},{"location":"composition/strategy-resources/","title":"Strategy Resources","text":"<p><code>StrategyResource</code> is the public record for one mounted resource:</p> <pre><code>from aaax import StrategyResource\n\n\nresource = StrategyResource(\n    kind=\"tactic\",\n    name=\"review\",\n    ref=\"psi://demo/analyst-pack/tactics/review\",\n    entrypoint=\"demo.tactics:review\",\n    description=\"Review one record batch.\",\n    metadata={\"runtime\": \"python\"},\n)\n</code></pre>"},{"location":"composition/strategy-resources/#fields","title":"Fields","text":"<p><code>kind</code> : The resource category. Built-in values are <code>tactic</code>, <code>channel</code>, <code>service</code>,   <code>schema</code>, <code>package</code>, <code>run</code>, <code>config</code>, <code>doc</code>, <code>example</code>, <code>snapshot</code>, <code>asset</code>,   and <code>custom</code>.</p> <p><code>name</code> : The local name used in the shell and HTTP paths.</p> <p><code>ref</code> : The stable package or service ref. Package imports preserve <code>psi://</code> refs.</p> <p><code>entrypoint</code> : Optional Python import path. Tactics use this to bind local handlers.</p> <p><code>description</code> : Short human-readable description.</p> <p><code>metadata</code> : Structured hints copied from package resources, such as runtime, schema refs,   examples, config defaults, service transport, package version, or package card.</p>"},{"location":"composition/strategy-resources/#local-name-versus-ref","title":"Local Name Versus Ref","text":"<p>The same package resource can have different local names in different shells:</p> <pre><code>shell.use_package(\"packages/review\", prefix=\"analysts\")\n</code></pre> <p>The tactic named <code>review</code> in the package becomes <code>analysts.review</code> locally, but its ref remains:</p> <pre><code>psi://demo/review/tactics/review\n</code></pre> <p>This lets a shell compose multiple packages without rewriting their package identity.</p>"},{"location":"composition/strategy-resources/#handler-binding","title":"Handler Binding","text":"<p>Resources are metadata by default. They become executable when AAAX stores a handler under the same local name:</p> <pre><code>strategy.tactic(\n    \"echo\",\n    ref=\"psi://demo/tools/tactics/echo\",\n    handler=lambda input_value, context=None: {\"echo\": input_value},\n)\n</code></pre> <p>Handlers may be synchronous or asynchronous. If they accept a <code>context</code> keyword, AAAX passes the request context.</p>"},{"location":"concepts/launch-boundary/","title":"Shell Boundary","text":"<p>AAAX is the shell between package metadata and a usable command surface. It prepares the environment a caller needs, but it does not try to become the caller.</p>"},{"location":"concepts/launch-boundary/#what-the-shell-does","title":"What The Shell Does","text":"<p>In the current surface, the shell does five things:</p> <ul> <li>Load a strategy file or package manifest.</li> <li>List and name the resources that belong to the application.</li> <li>Bind local handlers where the package provides local Python entrypoints.</li> <li>Prepare channel storage for package channels.</li> <li>Expose a FastAPI app with stable endpoints.</li> </ul> <p>The agent-facing layer can build on this later by reading the same resource map, package cards, docs, examples, and service URLs.</p>"},{"location":"concepts/launch-boundary/#what-aaax-avoids","title":"What AAAX Avoids","text":"<p>AAAX does not own:</p> <ul> <li>model provider configuration;</li> <li>prompt execution;</li> <li>tool permission policy;</li> <li>distributed scheduling;</li> <li>production deployment;</li> <li>package storage;</li> <li>long-term event retention.</li> </ul> <p>Those jobs belong to LLLM runtimes, SSSN stores, PsiHub, deployment platforms, and agent tools. AAAX keeps the shell boring enough that all of those systems can meet there.</p>"},{"location":"concepts/launch-boundary/#handoff-shape","title":"Handoff Shape","text":"flowchart LR   A[\"Package card\"] --&gt; E[\"Handoff context\"]   B[\"Resource refs\"] --&gt; E   C[\"FastAPI endpoints\"] --&gt; E   D[\"Docs and examples\"] --&gt; E   E --&gt; F[\"Human operator\"]   E --&gt; G[\"Coding agent\"]   E --&gt; H[\"Automation script\"] <p>The handoff is intentionally data-shaped: names, refs, docs, endpoints, and examples. That keeps AAAX compatible with many agent and IDE surfaces.</p>"},{"location":"concepts/package-composition/","title":"Package Composition","text":"<p>AAAX composes packages by mounting manifest resources into a shell. A package remains the unit of publication and discovery. A strategy is the shell session that chooses local names, binds handlers, and serves endpoints.</p> flowchart TD   A[\"psi.toml\"] --&gt; B[\"PsiHub manifest\"]   B --&gt; C[\"mounted resources\"]   C --&gt; D[\"Tactic handlers\"]   C --&gt; E[\"Channel handlers\"]   C --&gt; F[\"Docs, examples, assets\"]   D --&gt; G[\"FastAPI endpoints\"]   E --&gt; G   F --&gt; H[\"Agent or human context\"]"},{"location":"concepts/package-composition/#direct-package-shell","title":"Direct Package Shell","text":"<p><code>strategy_from_package(path)</code> creates one shell from one package:</p> <pre><code>from aaax import strategy_from_package\n\n\nshell = strategy_from_package(\"packages/analyst-pack\")\n</code></pre> <p>The shell name defaults to <code>package.name</code>. Package card metadata is copied onto the strategy metadata and package resource.</p>"},{"location":"concepts/package-composition/#imported-packages","title":"Imported Packages","text":"<p><code>Strategy.use_package(path)</code> mounts one package into an existing shell:</p> <pre><code>from aaax import Strategy\n\n\nshell = Strategy(\"analysis-shell\")\nshell.use_package(\"packages/sources\", prefix=\"sources\")\nshell.use_package(\"packages/analysts\", prefix=\"analysts\")\n</code></pre> <p>Prefixes only affect local shell names. The original <code>psi://</code> refs stay in the resource records.</p>"},{"location":"concepts/package-composition/#binding","title":"Binding","text":"<p>When <code>bind=True</code>, AAAX tries to bind:</p> <ul> <li>Python tactic entrypoints through LLLM's <code>Tactic</code> or <code>as_tactic</code> boundary.</li> <li>Package channels to a local SSSN <code>LocalStore</code>.</li> <li>Services that declare one <code>tactic</code> to the same tactic handler.</li> </ul> <p>Use <code>bind=False</code> when you only want the mount table and metadata:</p> <pre><code>shell.use_package(\"packages/remote-only\", bind=False)\n</code></pre> <p>That is useful for remote packages, planning tools, and agent handoff contexts where another process will resolve the actual service URLs.</p>"},{"location":"concepts/strategies/","title":"Strategies","text":"<p>A <code>Strategy</code> is an AAAX shell session. It is a mounted resource table plus an optional top-level command. The table describes resources; handlers make selected resources executable.</p> <pre><code>from aaax import Strategy\n\n\nshell = Strategy(\"analyst-shell\")\nshell.tactic(\n    \"review\",\n    ref=\"psi://society/analysts/tactics/review\",\n    description=\"Review one record batch.\",\n)\nshell.channel(\n    \"events\",\n    ref=\"psi://society/source/channels/events\",\n    description=\"Incoming source events.\",\n)\n</code></pre>"},{"location":"concepts/strategies/#resource-records","title":"Resource Records","text":"<p>Each resource has:</p> <ul> <li><code>kind</code>: package, tactic, channel, service, schema, run, config, doc, example,   snapshot, asset, or custom.</li> <li><code>name</code>: the local shell name used by endpoints.</li> <li><code>ref</code>: the stable external ref, usually a <code>psi://</code> ref.</li> <li><code>entrypoint</code>: the Python import target when a package resource is executable.</li> <li><code>description</code>: human-facing text.</li> <li><code>metadata</code>: package, runtime, schema, examples, card, and other structured hints.</li> </ul> <p>Names are local. Refs are stable. This distinction lets one shell mount two packages that both contain <code>events</code> by naming them <code>source.events</code> and <code>review.events</code> locally while retaining their package refs.</p>"},{"location":"concepts/strategies/#runner","title":"Runner","text":"<p>A runner is the shell's top-level <code>/run</code> command:</p> <pre><code>@shell.runner\ndef run(input_value, *, context=None):\n    return {\n        \"input\": input_value,\n        \"resource_count\": len(shell.resources),\n        \"context\": context or {},\n    }\n</code></pre> <p>If no runner is set, AAAX returns a shell pack containing the input and mounted resource list. That default makes inspection and early integration useful before the shell has a custom workflow.</p>"},{"location":"concepts/strategies/#resource-invocation","title":"Resource Invocation","text":"<p>Every mounted resource can be invoked:</p> <pre><code>curl -X POST http://127.0.0.1:8400/resources/review/invoke \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"records\": []}}'\n</code></pre> <p>When a resource has no handler, AAAX returns the resource metadata, input, and context. When a handler is bound, AAAX calls it and returns the output envelope.</p>"},{"location":"launch/","title":"Shell","text":"<p>Shell is the callable half of AAAX. After a package or strategy is loaded and resources are mounted, AAAX exposes the same strategy through surfaces for humans, applications, and coding agents.</p> flowchart LR   A[\"Strategy\"] --&gt; B[\"Mounted resources\"]   B --&gt; C[\"CLI shell\"]   B --&gt; D[\"FastAPI shell\"]   B --&gt; E[\"Agentic shell\"]   C --&gt; F[\"human inspect and serve\"]   D --&gt; G[\"apps and services\"]   E --&gt; H[\"Codex / Claude Code\"]"},{"location":"launch/#shell-surfaces","title":"Shell Surfaces","text":"<ul> <li>CLI Shell: a human-facing command surface for inspecting,   mounting, and serving packages.</li> <li>FastAPI Shell: an application-facing HTTP surface for   <code>/run</code>, tactics, channels, packages, and resource invocation.</li> <li>Agentic Shell: an agent-facing context surface for Codex,   Claude Code, IDE agents, and other coding-agent workflows.</li> </ul>"},{"location":"launch/#operational-boundary","title":"Operational Boundary","text":"<p>AAAX validates host, port, and log-level CLI inputs. It does not manage TLS, replicas, queue workers, secrets, containers, or cloud deployment. Keep those in the deployment layer and treat AAAX as the shell object that layer serves.</p>"},{"location":"launch/agent-handoff/","title":"Agentic Shell","text":"<p>AAAX is not an agent runtime, but it is designed so an agent can receive useful shell context without guessing.</p>"},{"location":"launch/agent-handoff/#what-an-agent-can-read","title":"What An Agent Can Read","text":"<p>An agent or IDE surface can call:</p> <pre><code>GET /strategy\nGET /resources\nGET /packages\nGET /tactics\nGET /channels\n</code></pre> <p>From those endpoints it can learn:</p> <ul> <li>which packages are present;</li> <li>which tactics are callable;</li> <li>which channels accept events;</li> <li>which services are mapped to tactics;</li> <li>where docs, examples, and assets live;</li> <li>what package cards suggest about safety, latency, and commands.</li> </ul>"},{"location":"launch/agent-handoff/#what-an-agent-can-do","title":"What An Agent Can Do","text":"<p>The same shell surface lets an agent:</p> <ul> <li>call one tactic with <code>/tactics/{name}/run</code>;</li> <li>invoke a named service through <code>/resources/{name}/invoke</code>;</li> <li>append an observation to a channel;</li> <li>query channel history;</li> <li>call <code>/run</code> for the strategy's intended top-level operation.</li> </ul>"},{"location":"launch/agent-handoff/#handoff-contract","title":"Handoff Contract","text":"flowchart TD   A[\"AAAX mount table\"] --&gt; D[\"Agent context\"]   B[\"Package card\"] --&gt; D   C[\"Docs and examples\"] --&gt; D   D --&gt; E[\"Choose endpoint\"]   E --&gt; F[\"Call tactic\"]   E --&gt; G[\"Append event\"]   E --&gt; H[\"Ask human\"] <p>The handoff contract is metadata plus endpoints. AAAX avoids telling the agent how to reason, but gives it a shell-shaped surface it can inspect before acting.</p>"},{"location":"launch/cli-shell/","title":"CLI Shell","text":"<p>The CLI shell is the human-facing AAAX surface. It keeps the workflow close to a normal shell habit: inspect what is mounted, then serve the same strategy for other callers.</p>"},{"location":"launch/cli-shell/#inspect","title":"Inspect","text":"<pre><code>aaax inspect packages/analyst-pack\naaax inspect strategy.py\n</code></pre> <p>Inspection prints the local shell names, resource kinds, and original <code>psi://</code> refs. Use it before running a package so name collisions, prefixes, and missing handlers are visible.</p>"},{"location":"launch/cli-shell/#serve","title":"Serve","text":"<pre><code>aaax serve strategy.py --host 127.0.0.1 --port 8400\naaax serve packages/analyst-pack --port 8400\n</code></pre> <p><code>launch</code> is currently an alias for <code>serve</code>:</p> <pre><code>aaax launch packages/analyst-pack --port 8400\n</code></pre>"},{"location":"launch/cli-shell/#script-shape","title":"Script Shape","text":"<p>For multi-package systems, a <code>strategy.py</code> file is the shell script:</p> <pre><code>from aaax import Strategy\n\n\ndef build_strategy() -&gt; Strategy:\n    shell = Strategy(\"analysis-shell\")\n    shell.use_package(\"packages/source-channels\", prefix=\"sources\")\n    shell.use_package(\"packages/analyst-tactics\", prefix=\"analysts\")\n    return shell\n</code></pre> <p>The CLI does not hide the package model. It makes the mounted strategy visible enough for a human to decide what to call next.</p>"},{"location":"launch/fastapi-surface/","title":"FastAPI Shell","text":"<p><code>create_strategy_app(strategy)</code> exposes the shell as a FastAPI application. Responses use a simple envelope:</p> <pre><code>{\n  \"output\": {},\n  \"strategy\": \"analysis-shell\",\n  \"metadata\": {}\n}\n</code></pre>"},{"location":"launch/fastapi-surface/#health-and-metadata","title":"Health And Metadata","text":"<pre><code>GET /health\nGET /strategy\nGET /resources\nGET /packages\nGET /tactics\nGET /channels\n</code></pre> <p>These endpoints are read-only and useful for smoke tests, UI surfaces, and agent handoff. They are the shell's <code>ls</code> and <code>env</code>.</p>"},{"location":"launch/fastapi-surface/#shell-run","title":"Shell Run","text":"<pre><code>POST /run\n</code></pre> <p>Request:</p> <pre><code>{\n  \"input\": {\"task\": \"summarize\"},\n  \"context\": {\"request\": \"demo\"}\n}\n</code></pre> <p><code>/run</code> calls the shell runner. If no runner is configured, it returns the default shell pack.</p>"},{"location":"launch/fastapi-surface/#tactic-run","title":"Tactic Run","text":"<pre><code>POST /tactics/{name}/run\n</code></pre> <p>This invokes the resource named <code>{name}</code> and marks the response metadata endpoint as <code>tactic</code>.</p>"},{"location":"launch/fastapi-surface/#generic-resource-invocation","title":"Generic Resource Invocation","text":"<pre><code>POST /resources/{name}/invoke\n</code></pre> <p>This is the universal escape hatch. It can invoke tactics, services, channels, or custom resources by local shell name.</p>"},{"location":"launch/fastapi-surface/#channel-events","title":"Channel Events","text":"<pre><code>POST /channels/{name}/events\nGET /channels/{name}/events\n</code></pre> <p>Append request:</p> <pre><code>{\n  \"input\": {\n    \"kind\": \"record\",\n    \"source\": \"client\",\n    \"payload\": {\"text\": \"hello\"},\n    \"metadata\": {\"demo\": true}\n  }\n}\n</code></pre> <p>Query parameters:</p> <ul> <li><code>after_cursor</code>: non-negative integer, default <code>0</code>.</li> <li><code>limit</code>: positive integer, default <code>100</code>.</li> <li><code>kind</code>: optional event kind filter.</li> </ul> <p>AAAX converts channel validation errors into <code>400</code> responses and missing resources into <code>404</code> responses.</p>"},{"location":"reference/cli/","title":"CLI","text":"<p>The <code>aaax</code> command is the shell entrypoint. It loads, inspects, and serves packages or strategy files.</p>"},{"location":"reference/cli/#inspect","title":"Inspect","text":"<pre><code>aaax inspect TARGET\naaax inspect TARGET --json\n</code></pre> <p><code>TARGET</code> can be a strategy file, package folder, direct <code>psi.toml</code>, directory, or <code>module:attribute</code> value.</p> <p>Text output lists resource kind, local name, and ref or entrypoint:</p> <pre><code>analysis-pack: Demo package.\npackage analysis-pack psi://demo/analysis-pack\ntactic echo psi://demo/analysis-pack/tactics/echo\nchannel events psi://demo/analysis-pack/channels/events\n</code></pre>"},{"location":"reference/cli/#serve","title":"Serve","text":"<pre><code>aaax serve TARGET --host 127.0.0.1 --port 8400 --log-level info\n</code></pre> <p>Validation:</p> <ul> <li>host must be a host name or address, not a URL;</li> <li>port must be between <code>1</code> and <code>65535</code>;</li> <li>log level must be one of <code>critical</code>, <code>debug</code>, <code>error</code>, <code>info</code>, <code>trace</code>, or   <code>warning</code>.</li> </ul>"},{"location":"reference/cli/#launch","title":"Launch","text":"<pre><code>aaax launch TARGET --port 8400\n</code></pre> <p><code>launch</code> is an alias for <code>serve</code> in the current public surface. It exists so the command family can grow without changing the shell habit: inspect, mount, serve.</p>"},{"location":"reference/http-api/","title":"HTTP API","text":"<p>AAAX exposes the shell surface as JSON endpoints through FastAPI.</p>"},{"location":"reference/http-api/#envelopes","title":"Envelopes","text":"<p>Requests use:</p> <pre><code>{\n  \"input\": {},\n  \"context\": {}\n}\n</code></pre> <p>Responses use:</p> <pre><code>{\n  \"output\": {},\n  \"strategy\": \"strategy-name\",\n  \"metadata\": {}\n}\n</code></pre>"},{"location":"reference/http-api/#endpoints","title":"Endpoints","text":"<p><code>GET /health</code> : Returns <code>{\"ok\": true, \"strategy\": \"&lt;name&gt;\"}</code>.</p> <p><code>GET /strategy</code> : Returns <code>StrategyInfo</code>.</p> <p><code>GET /resources</code> : Returns all resources.</p> <p><code>GET /packages</code> : Returns resources with kind <code>package</code>.</p> <p><code>GET /tactics</code> : Returns resources with kind <code>tactic</code>.</p> <p><code>GET /channels</code> : Returns resources with kind <code>channel</code>.</p> <p><code>POST /run</code> : Calls the shell runner.</p> <p><code>POST /resources/{name}/invoke</code> : Invokes any resource by local name.</p> <p><code>POST /tactics/{name}/run</code> : Invokes a tactic by local name.</p> <p><code>POST /channels/{name}/events</code> : Appends an event to a channel.</p> <p><code>GET /channels/{name}/events</code> : Queries channel events.</p>"},{"location":"reference/http-api/#errors","title":"Errors","text":"<p>Missing resources return <code>404</code>.</p> <p>Invalid channel query parameters or unsupported channel operations return <code>400</code>.</p> <p>Unhandled handler exceptions propagate through FastAPI's normal exception path, which is useful during local development and should be handled by deployment middleware in production.</p>"},{"location":"reference/python-api/","title":"Python API","text":"<p>AAAX's public Python API is intentionally compact. The objects still use the name <code>Strategy</code>, but conceptually a strategy is the shell session: a mounted resource table plus an optional top-level command.</p>"},{"location":"reference/python-api/#strategy","title":"Strategy","text":"<pre><code>from aaax import Strategy\n</code></pre> <p>Constructor:</p> <pre><code>Strategy(\n    name: str,\n    *,\n    description: str = \"\",\n    resources=(),\n    runner=None,\n    metadata=None,\n)\n</code></pre> <p>Common methods:</p> <ul> <li><code>strategy.tactic(name, **kwargs)</code></li> <li><code>strategy.channel(name, **kwargs)</code></li> <li><code>strategy.service(name, **kwargs)</code></li> <li><code>strategy.package(name, **kwargs)</code></li> <li><code>strategy.snapshot(name, **kwargs)</code></li> <li><code>strategy.resource(kind, name, **kwargs)</code></li> <li><code>strategy.add_resource(resource, handler=None)</code></li> <li><code>strategy.use_package(path, prefix=None, store=None, bind=True)</code></li> <li><code>strategy.runner(fn)</code></li> <li><code>await strategy.arun(input_value=None, context=None)</code></li> <li><code>await strategy.invoke_resource(name, input_value=None, context=None)</code></li> </ul>"},{"location":"reference/python-api/#models","title":"Models","text":"<pre><code>from aaax import StrategyResource, StrategyInfo\nfrom aaax import StrategyRunRequest, StrategyRunResponse\n</code></pre> <p><code>StrategyResource</code> describes one mounted resource. <code>StrategyInfo</code> describes a full shell session. <code>StrategyRunRequest</code> and <code>StrategyRunResponse</code> are the HTTP envelopes.</p>"},{"location":"reference/python-api/#loading","title":"Loading","text":"<pre><code>from aaax import load_strategy, strategy_from_package\n</code></pre> <p><code>load_strategy(target)</code> accepts:</p> <ul> <li>a directory containing <code>strategy.py</code>;</li> <li>a directory containing <code>aaax.py</code>;</li> <li>a directory containing <code>psi.toml</code>;</li> <li>a direct Python file;</li> <li>a direct <code>psi.toml</code> file;</li> <li>a <code>module:attribute</code> target.</li> </ul> <p><code>strategy_from_package(path, name=None, prefix=None, store=None, bind=True)</code> creates a shell directly from one PsiHub package.</p>"},{"location":"reference/python-api/#package-composition","title":"Package Composition","text":"<pre><code>from aaax import add_package\n</code></pre> <p><code>add_package(strategy, path, prefix=None, store=None, bind=True)</code> mutates and returns the given strategy.</p>"},{"location":"reference/python-api/#fastapi","title":"FastAPI","text":"<pre><code>from aaax import create_strategy_app\n\n\napp = create_strategy_app(strategy)\n</code></pre> <p>The app stores the original strategy at <code>app.state.aaax_strategy</code>.</p>"},{"location":"tutorials/channel-events/","title":"Channel Events","text":"<p>Package channels become SSSN-backed local endpoints when packages are mounted.</p>"},{"location":"tutorials/channel-events/#1-add-a-channel","title":"1. Add A Channel","text":"psi.toml<pre><code>[channels.events]\nform = \"log\"\ndescription = \"Application event stream.\"\n</code></pre> <p>AAAX creates the channel in the package <code>.sssn</code> store by default. You can pass a custom store path when mounting:</p> <pre><code>shell.use_package(\"packages/source-pack\", store=\".aaax/source-store\")\n</code></pre>"},{"location":"tutorials/channel-events/#2-append-an-event","title":"2. Append An Event","text":"<pre><code>curl -X POST http://127.0.0.1:8400/channels/events/events \\\n  -H 'content-type: application/json' \\\n  -d '{\n    \"input\": {\n      \"kind\": \"record\",\n      \"source\": \"tutorial\",\n      \"payload\": {\"text\": \"first\"}\n    }\n  }'\n</code></pre> <p>The response output is an SSSN event envelope with an id, cursor, timestamp, channel, kind, payload, and metadata.</p>"},{"location":"tutorials/channel-events/#3-query-events","title":"3. Query Events","text":"<pre><code>curl 'http://127.0.0.1:8400/channels/events/events?after_cursor=0&amp;limit=10'\n</code></pre> <p>Filter by kind:</p> <pre><code>curl 'http://127.0.0.1:8400/channels/events/events?kind=record&amp;limit=10'\n</code></pre>"},{"location":"tutorials/channel-events/#4-use-the-shell-escape-hatch","title":"4. Use The Shell Escape Hatch","text":"<pre><code>curl -X POST http://127.0.0.1:8400/resources/events/invoke \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"op\": \"query\", \"limit\": 10}}'\n</code></pre> <p>The generic path is the shell escape hatch for clients that do not want resource-kind-specific routes.</p>"},{"location":"tutorials/compose-packages/","title":"Compose Packages","text":"<p>Use a strategy file when the shell needs more than one package or custom top-level behavior.</p>"},{"location":"tutorials/compose-packages/#1-import-packages","title":"1. Import Packages","text":"strategy.py<pre><code>from aaax import Strategy\n\n\ndef build_strategy() -&gt; Strategy:\n    shell = Strategy(\n        \"analysis-shell\",\n        description=\"Mount source channels and analyst tactics.\",\n    )\n    shell.use_package(\"packages/source-pack\", prefix=\"source\")\n    shell.use_package(\"packages/analyst-pack\", prefix=\"analyst\")\n    return shell\n</code></pre> <p>The resource <code>events</code> from <code>source-pack</code> becomes <code>source.events</code>. The resource <code>echo</code> from <code>analyst-pack</code> becomes <code>analyst.echo</code>.</p>"},{"location":"tutorials/compose-packages/#2-add-a-runner","title":"2. Add A Runner","text":"strategy.py<pre><code>from aaax import Strategy\n\n\ndef build_strategy() -&gt; Strategy:\n    shell = Strategy(\"analysis-shell\")\n    shell.use_package(\"packages/source-pack\", prefix=\"source\")\n    shell.use_package(\"packages/analyst-pack\", prefix=\"analyst\")\n\n    @shell.runner\n    def run(input_value, *, context=None):\n        return {\n            \"task\": input_value,\n            \"resources\": [resource.name for resource in shell.resources],\n            \"context\": context or {},\n        }\n\n    return shell\n</code></pre>"},{"location":"tutorials/compose-packages/#3-serve-the-shell-and-call","title":"3. Serve The Shell And Call","text":"<pre><code>aaax serve strategy.py --port 8400\n</code></pre> <pre><code>curl -X POST http://127.0.0.1:8400/run \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"task\": \"review\"}}'\n</code></pre> <p>Call a prefixed tactic:</p> <pre><code>curl -X POST http://127.0.0.1:8400/tactics/analyst.echo/run \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"text\": \"hello\"}}'\n</code></pre> <p>Prefixes use dots in local names. FastAPI path variables preserve those names.</p>"},{"location":"tutorials/serve-package/","title":"Serve A Package","text":"<p>This tutorial creates a tiny package, mounts it in the AAAX shell, and serves it.</p>"},{"location":"tutorials/serve-package/#1-create-files","title":"1. Create Files","text":"<pre><code>mkdir -p analyst-pack/demo\ntouch analyst-pack/demo/__init__.py\n</code></pre> analyst-pack/demo/tactics.py<pre><code>def echo(input_value, *, context=None):\n    metadata = getattr(context, \"metadata\", {})\n    return {\"echo\": input_value, \"metadata\": metadata}\n</code></pre> analyst-pack/psi.toml<pre><code>[package]\npsi_version = \"0.1\"\norg = \"demo\"\nname = \"analyst-pack\"\nversion = \"0.1.0\"\nkind = \"app\"\nprimary = \"runs.local\"\ndescription = \"Demo analyst package.\"\n\n[card]\nsummary = \"Small local package for AAAX.\"\ntags = [\"demo\", \"analysis\"]\nsuggested_commands = [\"aaax serve .\"]\n\n[tactics.echo]\nentry = \"demo.tactics:echo\"\nruntime = \"python\"\ndescription = \"Echo one payload.\"\n\n[channels.events]\nform = \"log\"\ndescription = \"Application event stream.\"\n\n[services.api]\ntactic = \"echo\"\ntransport = \"fastapi\"\ndescription = \"HTTP-facing echo service.\"\n\n[runs.local]\ntactics = [\"echo\"]\nchannels = [\"events\"]\nservices = [\"api\"]\n</code></pre>"},{"location":"tutorials/serve-package/#2-inspect","title":"2. Inspect","text":"<pre><code>aaax inspect analyst-pack\n</code></pre> <p>Expected resources include package, tactic, channel, service, and run records. This is the shell's first move: list what is mounted before you run it.</p>"},{"location":"tutorials/serve-package/#3-open-the-shell-surface","title":"3. Open The Shell Surface","text":"<pre><code>aaax serve analyst-pack --port 8400\n</code></pre>"},{"location":"tutorials/serve-package/#4-call","title":"4. Call","text":"<pre><code>curl -X POST http://127.0.0.1:8400/tactics/echo/run \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"text\": \"hello\"}, \"context\": {\"request\": \"tutorial\"}}'\n</code></pre> <p>The output should echo your input and include the context metadata.</p>"},{"location":"tutorials/tactic-endpoint/","title":"Tactic Endpoint","text":"<p>Tactics are the most direct executable resources mounted in the AAAX shell.</p>"},{"location":"tutorials/tactic-endpoint/#1-write-a-tactic","title":"1. Write A Tactic","text":"demo/tactics.py<pre><code>def classify(input_value, *, context=None):\n    text = input_value.get(\"text\", \"\")\n    label = \"empty\" if not text.strip() else \"text\"\n    return {\"label\": label, \"text\": text}\n</code></pre>"},{"location":"tutorials/tactic-endpoint/#2-declare-it","title":"2. Declare It","text":"psi.toml<pre><code>[tactics.classify]\nentry = \"demo.tactics:classify\"\nruntime = \"python\"\ndescription = \"Classify one input payload.\"\n</code></pre>"},{"location":"tutorials/tactic-endpoint/#3-serve-the-shell-and-call","title":"3. Serve The Shell And Call","text":"<pre><code>aaax serve . --port 8400\n</code></pre> <pre><code>curl -X POST http://127.0.0.1:8400/tactics/classify/run \\\n  -H 'content-type: application/json' \\\n  -d '{\"input\": {\"text\": \"hello\"}}'\n</code></pre>"},{"location":"tutorials/tactic-endpoint/#context","title":"Context","text":"<p>Request context is passed as LLLM <code>CallContext.metadata</code> when LLLM is installed:</p> <pre><code>curl -X POST http://127.0.0.1:8400/tactics/classify/run \\\n  -H 'content-type: application/json' \\\n  -d '{\n    \"input\": {\"text\": \"hello\"},\n    \"context\": {\"request\": \"trace-me\"}\n  }'\n</code></pre> <p>Plain callables can accept <code>context=None</code>; LLLM tactic subclasses receive the same context through their <code>run</code> or <code>arun</code> boundary.</p>"}]}